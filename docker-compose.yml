services:
  app:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./storage:/storage
    environment:
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
    restart: unless-stopped
